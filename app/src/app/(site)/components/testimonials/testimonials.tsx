import { Container } from '@/components/layout';
import { SectionHeader } from '@/components/ui';

const Testimonials = () => {
  return (
    <section id="testimonials" className='pt-16 pb-32'>
      <Container>
        <SectionHeader title="Testimonials" />
      </Container>
    </section>
  );
};

export default Testimonials;
