import { Container } from '../layout';

const Portfolio = () => {
  return (
    <section id="portfolio" className=' py-16'>
      <Container>
        <div>
          <h2 className="uppercase text-3xl font-extrabold">Some projects I have worked on</h2>
        </div>
      </Container>
    </section>
  );
};

export default Portfolio;
